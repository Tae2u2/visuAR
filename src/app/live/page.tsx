import AnimalEarsLiveWithSelector from "@/features/live/LiveShowView";

const LivePage = () => {
  return (
    <div>
      <AnimalEarsLiveWithSelector />
    </div>
  );
};

export default LivePage;
